# 売買チェック手順書

この文書は、`TRADING_RULE.md` で定めたルールを**実際の売買の場面でどう
チェックするか**を、手順として具体的にまとめたものです。ツールの中身を
知らない人でも、この手順に沿って確認すれば同じ判断ができることを
目指しています。

---

## 全体の流れ

```
[毎日 ひけ後]
   自動でデータ収集 + スクリーニング(screening_latest.csv が更新される)
        ↓
[STEP 1] 候補リストを確認する
        ↓
[STEP 2] 個別銘柄を目視でチェックする(自動判定を鵜呑みにしない)
        ↓
[STEP 3] 買う場合、注文を出す
        ↓
[STEP 4] 保有中、毎日チェックする
        ↓
[STEP 5] 手仕舞う(利確・損切り・期限切れ)
```

---

## STEP 1: 候補リストを確認する

毎日の取引が終わった後(日本時間16時以降)、自動的に更新される
`analysis_output/screening_latest.csv` を開きます。GitHubの
リポジトリページでも、パソコンにダウンロードしても確認できます。

このファイルには、その日の時点で「エントリー条件をすべて満たした銘柄」
が、**総合スコア(業種・信頼度・チャートの締まり具合を加味した期待値)**
の高い順に並んでいます。

### 見るべき列

| 列名 | 意味 | チェックポイント |
|---|---|---|
| `code` / `company_name` | 銘柄コード・会社名 | - |
| `close` | 本日の終値 | この価格を基準にエントリーを検討する |
| `expected_win_rate_pct` | 過去の同条件での勝率 | 50%を大きく下回っていないか |
| `expected_return_pct` | 過去の同条件での期待値(件数が少ない場合は全体平均へ補正済み) | - |
| `sector_adjustment_pct` | 業種による調整分 | マイナスが大きい業種(過去の実績が悪い業種)は要注意 |
| `tightness_bonus_pct` | チャートの締まり具合による加点 | プラスが大きいほど、値動きが穏やかな「タイトなベース」からの候補 |
| `composite_score` | 上記3つを合計した総合スコア | **この列でソートされている。基本はこの順に検討する** |
| `expected_value_sample_size` | 期待値の元になったデータ件数(n) | **30件以上あるか確認**。件数が少なすぎる(10件未満など)場合、参考程度に留める |
| `stop_loss_price` / `take_profit_price` | ルール通りの損切り・利確の目安価格 | 実際の注文価格の参考にする |

### この時点での絞り込み

- `composite_score` がマイナスの銘柄は**除外**する
- `expected_value_sample_size` が極端に少ない(目安: 30件未満)銘柄は、参考情報として見るに留め、優先度を下げる
- 同じ業種の銘柄が多数並んでいる場合、その業種に資金が偏りすぎないよう注意する(`TRADING_RULE.md` セクション5参照)

---

## STEP 2: 個別銘柄を目視でチェックする

自動スクリーニングは「数値条件」だけを見ています。**実際に買う前に、必ず
チャートを目で見て確認してください。** 以下は、機械的な条件だけでは
拾いきれない、注意すべきパターンです。

### 買ってもよいと判断できる状態(良いチャートの例)

- 直近のチャートで、**急激な上下動が少なく**、比較的なだらかに右肩上がりになっている
- 出来高が極端に少なくない(閑散株ではない)
- ニュースや適時開示を確認し、**株式分割・株式併合・増資などのイベントが直近にない**(あるとデータの計算がずれている可能性がある)
- 決算発表の直前・直後で無い(決算をまたぐと値動きが読みにくくなるため、決算日を事前に確認する)

### 買うのを避けるべき状態(注意すべきパターン)

- チャートが**すでに何度も乱高下**しており、値動きが荒い
- 直近で**大きな窓を開けて急騰**しており、そのまま高値づかみになりそうな形
- 出来高が**平常時と比べて極端に(5倍・10倍以上)急増**している日(何らかの材料が出て一時的に跳ねている可能性があり、その反動が読みにくい)
- 決算発表・重要な適時開示の**直前**である
- 同じ銘柄で**すでにポジションを持っている**(重複エントリーを避ける)

**迷ったら見送る**のが基本方針です。無理に全部の候補を買う必要はありません。

---

## STEP 3: 買う場合、注文を出す

1. STEP 1のリストにある`close`(前日終値)を参考に、指値・成行を判断する
2. 購入株数は、**損切りライン(`stop_loss_price`)に達した場合の損失額が、総資金に対して許容できる範囲**になるよう決める(`TRADING_RULE.md` セクション5参照。目安: 1トレードあたりの損失が総資金の1〜2%程度に収まる株数)
3. 可能であれば、購入と同時に**損切りの逆指値注文**を入れておく(感情的な判断でルールを破らないための工夫)

---

## STEP 4: 保有中、毎日チェックする

保有している間は、毎日以下を確認します。

- 株価が損切りライン(`stop_loss_price`)を下回っていないか
- 株価が利確ライン(`take_profit_price`)に到達していないか
- 保有期間が20営業日(約1ヶ月)を超えていないか
- (任意)決算発表など、当初の判断根拠を覆すような新しい材料が出ていないか

---

## STEP 5: 手仕舞う

以下のいずれかに該当したら、**機械的に**手仕舞います。ここで「もう少し
待てば戻るかもしれない」と判断を先延ばしにしないことが、損切りルールを
守る上で最も重要です。

| 条件 | 行動 |
|---|---|
| 株価が損切りラインに到達した | **即座に売却する**(理由を問わず) |
| 株価が利確ラインに到達した | 売却する(一部だけ利確して残りを持ち続ける、という判断も可) |
| 20営業日が経過し、どちらにも届いていない | その時点の株価で手仕舞う(ルール上の「タイムアウト」) |

---

## よくある失敗パターン(避けるべきこと)

- **損切りラインを「もう少しだけ」延長してしまう**: `TRADING_RULE.md`のデータは、決めたルールを機械的に守った場合の成績です。ルールを破ると、この文書の期待値は成立しなくなります
- **期待値がマイナスの銘柄でも、なんとなく良さそうだからと買ってしまう**: 感覚ではなく、STEP 1の数値を優先する
- **一つの業種・一つの銘柄に集中投資してしまう**: `TRADING_RULE.md`セクション5の分散の原則を守る
- **含み益が出ている銘柄を、利確ラインに達する前に「まだ上がりそうだから」と保有期間を延長する**: 保有期間を延ばす判断は、事前に決めたルールの変更になるため、行う場合は「なぜ延長するのか」を明文化した上で慎重に判断する
